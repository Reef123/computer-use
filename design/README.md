# design

Scratchpad. These are the working notes behind the top-level README. Raw, not polished.

They run a dual register. Confident on the reasoning. Honest on what is still unknown. The
depth labels (BUILD, INTERFACE, PROVISIONAL) mark which is which.

- `00_consolidated_spec.md`. The architecture as first settled. The plan.
- `00b_vision_v2_two_sensors.md`. Where the evidence moved it. One loop, two sensors, two routers.

The rest of the derivation is kept offline. The build plan, the decision trees, the
handoff briefs, the estimator spec.

Start with the top-level README. Come here for how it was reasoned.
